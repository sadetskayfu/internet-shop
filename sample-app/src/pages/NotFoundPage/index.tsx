import React from 'react';
import { INotFoundPageProps } from './interface';

const NotFoundPage: React.FC<INotFoundPageProps> = () => {
    return (
        <div>
            NotFoundPage
        </div>
    );
};

export default NotFoundPage;